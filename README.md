# React Native Challenge version eject

```
Find .apk /android/app/build/outputs/apk
```
###  To do:

Server with socket (WebSockets or Sockets.io) and implement in RN side, in order to allow users join to specific workout

